import React from "react";

const SearchBar = () => {
  return (
    <div>
      <input type="text" />
      <input type="checkbox" />
      Only show products in stock
    </div>
  );
};

export default SearchBar;
